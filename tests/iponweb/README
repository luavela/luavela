This directory used to contain tests that are written by IPONWEB employees as
uJIT project evolves. However, currently we have migrated to the unified layout
of test suites and the only thing left here is a set of performance benchmarks
equipped with a simple legacy runner script written in Perl.

Do not place new suites here, the layout for the benchmarks is to be refactored
anyway.
